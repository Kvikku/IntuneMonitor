# Architecture

> **For coding conventions, patterns, and guardrails** used by AI coding agents, see [`.github/copilot-instructions.md`](../.github/copilot-instructions.md).

## Project Structure

```
src/IntuneMonitor/
├── Authentication/     Credential creation (secret, certificate, device code)
│   └── CredentialFactory.cs
├── Commands/           Command implementations
│   ├── AuditLogCommand.cs
│   ├── CliHelpers.cs
│   ├── CommandBuilder.cs      (also registers `validate` inline via BackupValidator)
│   ├── DependencyCommand.cs
│   ├── DiffCommand.cs
│   ├── EntraMonitorCommand.cs
│   ├── ExportCommand.cs
│   ├── GlobalOptions.cs
│   ├── ImportCommand.cs
│   ├── MonitorCommand.cs
│   └── RollbackCommand.cs
├── Comparison/         Deep diff engine (split into focused classes)
│   ├── AssignmentComparer.cs
│   ├── ChangeBuilder.cs
│   ├── EntraSnapshotComparer.cs
│   ├── FieldComparer.cs
│   └── PolicyComparer.cs
├── Config/             Strongly-typed configuration POCOs
│   └── AppConfiguration.cs
├── Graph/              Microsoft Graph API client layer
│   ├── AuditLogFetcher.cs
│   ├── DirectoryAuditFetcher.cs
│   ├── EntraExporter.cs
│   ├── GraphClientFactory.cs
│   ├── GraphRetryHandler.cs
│   ├── GraphSubscriptionManager.cs
│   ├── IntuneExporter.cs
│   ├── IntuneImporter.cs
│   ├── JsonDefaults.cs
│   └── JsonElementHelpers.cs
├── Models/             Data models
│   ├── AuditModels.cs
│   ├── BackupModels.cs
│   ├── EntraAppModels.cs
│   ├── ImportResult.cs
│   └── IntuneContentTypes.cs
├── Notifications/      Drift detection alerting
│   ├── EmailNotificationSender.cs
│   ├── INotificationSender.cs
│   ├── NotificationFactory.cs
│   ├── NotificationService.cs
│   ├── SlackWebhookSender.cs
│   ├── TeamsWebhookSender.cs
│   └── WebhookNotificationSender.cs
├── Reporting/          Report generators (HTML, CSV, JSON)
│   ├── CsvReportGenerator.cs
│   ├── HtmlAuditReportGenerator.cs
│   ├── HtmlEntraReportGenerator.cs
│   ├── HtmlExportReportGenerator.cs
│   ├── HtmlReportGenerator.cs
│   ├── HtmlReportHelpers.cs
│   ├── HtmlTheme.cs
│   ├── MarkdownEntraReportGenerator.cs
│   ├── ReportPath.cs
│   └── ReportWriter.cs
├── Storage/            Backup storage backends
│   ├── AzureBlobStorage.cs
│   ├── BackupFileHelpers.cs
│   ├── BackupStorageFactory.cs
│   ├── BackupValidator.cs
│   ├── EntraSnapshotStorage.cs
│   ├── GitStorage.cs
│   ├── IBackupStorage.cs
│   └── LocalFileStorage.cs
├── UI/                 Terminal UI (Spectre.Console)
│   ├── ConsoleUI.cs
│   ├── InteractiveMenu.cs
│   └── MenuConstants.cs
└── Program.cs          Entry point — CLI routing + interactive menu
```

## Key Design Decisions

### Dual-Mode Entry Point

`Program.cs` checks `args.Length`:
- **No arguments** → Launches the interactive menu (`InteractiveMenu`)
- **With arguments** → Routes through `System.CommandLine` for direct CLI execution

Both paths share the same `AppConfiguration`, command classes, and logger factory.

### Storage Abstraction

`IBackupStorage` abstracts the backup backend:

```
IBackupStorage
├── LocalFileStorage     (writes JSON to timestamped directories)
├── GitStorage           (writes JSON + git commit + push)
└── AzureBlobStorage     (uploads JSON to Azure Blob Storage)
```

`BackupStorageFactory` selects the implementation based on `Backup.StorageType`.

### Content Type Registry

`IntuneContentTypes` is the central registry mapping each content type to:
- Its Microsoft Graph API endpoint
- Its JSON backup filename
- Its backup folder name

Adding a new content type only requires adding entries to these dictionaries.

### Comparison Engine

The comparison engine is split into focused classes:

- **`PolicyComparer`** — orchestrator that drives the comparison between live and backup data
- **`FieldComparer`** — computes field-level JSON differences between two policy snapshots
- **`AssignmentComparer`** — produces human-readable diffs for policy assignment arrays
- **`ChangeBuilder`** — factory for constructing `PolicyChange` instances with consistent severity assignment

The pipeline:

1. **Index** both sets by policy ID
2. **Added** = IDs in live but not backup
3. **Removed** = IDs in backup but not live
4. **Modified** = IDs in both, with deep JSON property comparison via `FieldComparer`

The comparer ignores volatile fields (`lastModifiedDateTime`, `version`, `@odata.type`) and handles assignment arrays with special human-readable formatting via `AssignmentComparer`.

### Reporting Pipeline

Each command can produce:
- **Console output** — structured logging via `ILogger` + styled tables via `ConsoleUI`
- **JSON reports** — serialized `ChangeReport` / `ExportReport` objects via `ReportWriter`
- **HTML reports** — self-contained dashboards generated by `HtmlReportGenerator` / `HtmlExportReportGenerator` / `HtmlAuditReportGenerator`, sharing utilities via `HtmlReportHelpers`
- **CSV reports** — tabular data via `CsvReportGenerator` with formula injection protection

### Entra App Monitoring Pipeline

A standalone pipeline for monitoring Entra app registrations and enterprise applications, completely separate from the Intune backup/monitor pipeline:

- **`EntraExporter`** — fetches `/applications` and `/servicePrincipals` with `$expand` for owners and appRoleAssignments; per-item calls only for `oauth2PermissionGrants` (with pacing)
- **`DirectoryAuditFetcher`** — queries `GET /auditLogs/directoryAudits` (v1.0) for 12 monitored activity types
- **`EntraSnapshotStorage`** — local-only timestamped JSON snapshots (not synced to Git/Blob)
- **`EntraSnapshotComparer`** — detects added/removed apps, owner changes, and permission changes
- **`EntraMonitorCommand`** — orchestrates the full flow: export → compare → audit → report → notify
- **`HtmlEntraReportGenerator`** / **`MarkdownEntraReportGenerator`** — combined report with snapshot changes and audit events

Entra data is **not** registered in `IntuneContentTypes` — it has its own models (`EntraAppModels.cs`), storage, and comparison logic. Notifications reuse the existing `NotificationService` by converting `EntraMonitorReport` to a `ChangeReport`.

## Extensibility

### Adding a New Content Type

1. Add a constant to `IntuneContentTypes`
2. Add entries to the `GraphEndpoints`, `FileNames`, and `FolderNames` dictionaries
3. That's it — export, import, monitor, and the interactive menu will pick it up automatically

### Adding a New Command

1. Create a new class in `Commands/` following the `ExportCommand` pattern (constructor takes `AppConfiguration` + `ILoggerFactory`)
2. Register the command in `CommandBuilder.cs`
3. Add a menu option constant in `MenuConstants.cs` and wire it in `InteractiveMenu.cs`
4. Update `docs/commands.md` and the commands table in `.github/copilot-instructions.md`

### Notification System

Drift alerts are sent via `INotificationSender` implementations:

- **`TeamsWebhookSender`** / **`SlackWebhookSender`** — extend `WebhookNotificationSender` base class
- **`EmailNotificationSender`** — sends via SMTP

`NotificationFactory` creates configured senders based on `AppConfiguration.Notifications`. `MonitorCommand` invokes them after drift detection.

## Dependencies

| Package | Version | Purpose |
|---|---|---|
| `Azure.Identity` | 1.13.2 | Entra ID authentication |
| `Microsoft.Extensions.Configuration.*` | 8.0.0 | Configuration loading |
| `Microsoft.Extensions.Logging.*` | 8.0.0 | Structured logging |
| `Spectre.Console` | 0.49.1 | Rich terminal UI |
| `System.CommandLine` | 2.0.0-beta4 | CLI parsing |

## Testing

Tests live in `tests/IntuneMonitor.Tests/` and use xUnit (25 test files):

```bash
dotnet test
```

Tests cover the diff engine, Graph API clients (via mock `HttpMessageHandler`), commands, storage backends, notifications, configuration, and UI constants. Test helpers include `GraphTestHelpers` for creating fake `GraphClientFactory` instances and `MockHttpHandler` for stubbing HTTP responses.

### Graph API URL Constants

All Microsoft Graph API base URLs are centralized in `GraphClientFactory`:
- `GraphBetaBaseUrl` — `https://graph.microsoft.com/beta`
- `GraphV1BaseUrl` — `https://graph.microsoft.com/v1.0`

All endpoint URLs in `IntuneExporter`, `IntuneImporter`, `AuditLogFetcher`, and `GraphSubscriptionManager` reference these constants rather than hardcoding URLs.
